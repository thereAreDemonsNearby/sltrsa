#!/bin/python3
import os

BL = 256;
with open('mulspeed.log', 'w') as f:
    while BL <= 8192:
        os.chdir("../")
        os.system("make -B mulspeed BITLEN=" + str(BL));
        os.chdir("test/")
        f.write(os.popen("./mulspeed").read())
        BL = BL + BL
